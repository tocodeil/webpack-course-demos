# Alias

1. When to alias?

2. Using resolve.alias
















Useful Snippets

const buildType = process.env.BROWSER ? 'browser' : 'node';

resolve: {
alias: {
actions: '...',
       },
         },
